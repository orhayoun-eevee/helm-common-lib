name: Publish libChart

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: on-tag-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish-libchart:
    name: Publish libChart to GHCR
    timeout-minutes: 30
    uses: orhayoun-eevee/build-workflow/.github/workflows/release-chart.yaml@3a50b1646217b7f3e02feb0a7263af66a5cea2e2
    with:
      chart_path: libChart
      release_environment: production
      enable_signing: true
