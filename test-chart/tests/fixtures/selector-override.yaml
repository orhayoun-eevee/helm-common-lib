global:
  name: test-app
  namespace: test-namespace
  labels:
    overrides:
      app.kubernetes.io/name: custom-name
workload:
  type: deployment
  spec:
    containers:
      app:
        enabled: true
        image:
          repository: nginx
          tag: "1.25"
