# Test: workload selector is required
global:
  name: test-app
  namespace: test-namespace
deployment:
  containers:
    app:
      enabled: true
      image:
        repository: nginx
        tag: "1.25"
