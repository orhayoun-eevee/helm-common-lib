# Test: workload.type must be a supported enum value
global:
  name: test-app
  namespace: test-namespace
workload:
  type: ""
deployment:
  containers:
    app:
      enabled: true
      image:
        repository: nginx
        tag: "1.25"
