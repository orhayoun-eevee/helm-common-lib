# Test: workload.type must be a supported enum value
global:
  name: test-app
  namespace: test-namespace
workload:
  type: ""
  spec:
    containers:
      app:
        enabled: true
        image:
          repository: nginx
          tag: "1.25"
