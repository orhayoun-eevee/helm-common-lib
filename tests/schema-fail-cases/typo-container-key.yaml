# Test: typo in deployment container object key should be rejected
global:
  name: test-app
  namespace: test-namespace

workload:
  type: deployment
  spec:
    containers:
      app:
        enabled: true
        image:
          repository: nginx
          tag: "1.25"
        imagPullPolicy: Always
