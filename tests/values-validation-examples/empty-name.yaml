# Intentionally invalid: empty global.name (used to test template validation error message).
# Run: helm template test . -f ../tests/values-validation-examples/empty-name.yaml
# Expected: Error about global.name is required and cannot be empty

global:
  name: ""
  namespace: test-namespace

deployment:
  replicas: 1
  containers:
    app:
      enabled: true
      image:
        repository: nginx
        tag: "1.25"
